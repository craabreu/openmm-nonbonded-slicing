name: build
channels:
  - conda-forge
dependencies:
  - cmake
  - clang @COMPILER_VERSION@
  - clang-tools @COMPILER_VERSION@
  - compiler-rt @COMPILER_VERSION@
  - llvm-openmp @COMPILER_VERSION@
  - khronos-opencl-icd-loader
  - make
  - openmm @OPENMM_VERSION@
  - pip >=22.2
  - pocl
  - pytest
  - python
  - doxygen
  - swig =4.0
  - numpy